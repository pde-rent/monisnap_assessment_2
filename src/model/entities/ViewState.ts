
// matches navigation (ideally we should use Vuex for this but let's keep it simple)

export enum ViewState {
    HOME = 0,
    TRANSFERS = 1,
    RECIPIENTS = 2
}

// we could manage the carousel state from jhere as well
